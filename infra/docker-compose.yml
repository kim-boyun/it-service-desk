services:
  api:
    build:
      context: ../apps/api
    environment:
      JWT_SECRET: "change-me"
      JWT_EXPIRES_MIN: "120"
      ALLOWED_EMAIL_DOMAINS: "kdischool.ac.kr"
    ports:
      - "8000:8000"
    command: >
      uvicorn app.main:app --host 0.0.0.0 --port 8000
    env_file:
      - .env
    volumes:
      - ./data/uploads:/data/uploads
      - ${USERPROFILE:-C:\Users\kdischool}\.aws:/root/.aws:ro
